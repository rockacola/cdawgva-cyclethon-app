# Base URL of the Cloudflare R2 public bucket serving donation data
NEXT_PUBLIC_R2_BASE_URL=

# Tiltify webhook signing ID (from Application > Webhooks > endpoint > Setup tab)
TILTIFY_WEBHOOK_SIGNING_ID=
